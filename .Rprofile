# I had to remove the renv/activate.R line because it was causing issues with R resulting in a corrupted R session. Will try to fix this later; sorry for the inconvenience.
#source("renv/activate.R")
